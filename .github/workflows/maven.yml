name: Build & Publish Javadoc

on:
  push:
    branches: [ javadocbackend ]   # change to master if needed
  workflow_dispatch:

permissions:
  contents: write        # needed to push to gh-pages

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      # 🔸 Run Maven in the backend module
      - name: Generate Javadoc (backend)
        run: |
          mvn -v
          mvn -B -DskipTests \
            -Dmaven.javadoc.failOnError=false \
            -DadditionalJOption=-Xdoclint:none \
            -pl backend -am javadoc:javadoc

          echo "Searching for apidocs…"
          find . -type d -name apidocs -print


      - name: Publish Javadoc to gh-pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: docs  # <-- correct path for your repo
          clean: true
          single-commit: true