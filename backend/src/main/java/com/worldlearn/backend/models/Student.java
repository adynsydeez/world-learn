package com.worldlearn.backend.models;

public class Student extends User {

    public Student(String email, String password, String first, String last, String role) {
        super(email, password, first, last, role);
    }
}
